<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quickquizapp">

    <application
        android:allowBackup="true"
        android:label="Quick Quiz"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.QuickQuizApp">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ChangePasswordActivity" />
        <activity android:name=".ChiTietBaiKiemTraActivity" />
        <activity android:name=".ClassListActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".HoatDongActivity" />
        <activity android:name=".NotificationActivity" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".SearchActivity" />

    </application>
</manifest>
